{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile and Run Sudoku",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/sudoku",
                "-lncurses"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Компиляция с ncurses"
        }
    ]
}
